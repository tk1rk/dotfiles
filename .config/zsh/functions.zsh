#!/usr/bin/env zsh

SAVEIFS=$IFS
IFS="$(pr  

function apt-impgpg --keyserver subkeys.pgp.net --recv-keys $1 | gpg --armor --export $1 | sudo apt-key add -
}

# reloads all funf=(~/.config/zsh/functunfunction $f:t autoload -U $f:t
}

# creates new script, populating shebang line & executable.if i=printf '#!/usr/bin/env %s\n\n' $1 > $2 && chmod 755 $2 && vim + $2 && cecho "'which' could not find $1, is it in yo# in c is in path, this throw out the command ha#over  (man rehash
}
